#include <iostream>
#include <string>
#include <algorithm>
#include "hash.h"
using namespace std;

class Brute
{
public:
	static string bruteattack(string hash, bool visualization, string hashType);
};